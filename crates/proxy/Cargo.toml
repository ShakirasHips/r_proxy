[package]
name = "proxy"
version = "0.1.0"
edition = "2024"

[dependencies]
serde = { version = "1.0.228", features = ["std", "derive"] }
toml = "1.1.2"
anyhow = "1.0.104"
chrono = "0.4.45"
tokio = {  version = "1.53.1", features = ["full"] }
